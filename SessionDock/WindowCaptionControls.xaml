<UserControl x:Class="SessionDock.WindowCaptionControls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Focusable="False">
    <UserControl.Resources>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource UtilityTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface"
                                CornerRadius="7"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background"
                                        Value="{DynamicResource CaptionButtonHoverBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource CaptionButtonHoverTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Surface" Property="Background"
                                        Value="{DynamicResource CaptionButtonPressedBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource CaptionButtonHoverTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value=".4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseCaptionButton" TargetType="Button"
               BasedOn="{StaticResource CaptionButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface"
                                CornerRadius="7"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background"
                                        Value="{DynamicResource CaptionCloseHoverBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource CaptionCloseHoverTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Surface" Property="Background"
                                        Value="{DynamicResource CaptionClosePressedBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource CaptionCloseHoverTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value=".4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <Button x:Name="MinimizeButton"
                Style="{StaticResource CaptionButton}"
                Click="MinimizeButton_Click"
                AutomationProperties.Name="{DynamicResource Caption.MinimizeName}"
                ToolTip="{DynamicResource Caption.Minimize}">
            <Path Width="12" Height="12" Stretch="Uniform"
                  Data="M 2,8 H 14"
                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                  StrokeThickness="1.5" StrokeStartLineCap="Square"
                  StrokeEndLineCap="Square" IsHitTestVisible="False"/>
        </Button>
        <Button x:Name="MaximizeButton"
                Style="{StaticResource CaptionButton}"
                Click="MaximizeButton_Click"
                AutomationProperties.Name="{DynamicResource Caption.MaximizeName}"
                ToolTip="{DynamicResource Caption.Maximize}">
            <Grid Width="12" Height="12" IsHitTestVisible="False">
                <Path x:Name="MaximizeGlyph" Stretch="Uniform"
                      Data="M 2.5,2.5 H 13.5 V 13.5 H 2.5 Z"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.3" StrokeLineJoin="Miter"/>
                <Path x:Name="RestoreGlyph" Stretch="Uniform" Visibility="Collapsed"
                      Data="M 5,2.5 H 13.5 V 11 M 2.5,5 H 11 V 13.5 H 2.5 Z"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.3" StrokeLineJoin="Miter"/>
            </Grid>
        </Button>
        <Button x:Name="CloseButton"
                Style="{StaticResource CloseCaptionButton}"
                Click="CloseButton_Click"
                AutomationProperties.Name="{DynamicResource Caption.CloseName}"
                ToolTip="{DynamicResource Caption.Close}">
            <Path Width="12" Height="12" Stretch="Uniform"
                  Data="M 3,3 L 13,13 M 13,3 L 3,13"
                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                  StrokeThickness="1.5" StrokeStartLineCap="Square"
                  StrokeEndLineCap="Square" IsHitTestVisible="False"/>
        </Button>
    </StackPanel>
</UserControl>
