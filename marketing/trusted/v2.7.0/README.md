# Trusted SessionDock 2.7.0 marketing assets

[Install Latest SessionDock release](https://github.com/Makmatoe/SessionDock/releases/latest/download/SessionDock-win-x64-Setup.exe)

These files are byte-identical mirrors of the reviewed assets under
`docs/images/sessiondock-v2.7.0`. Only this trusted directory may be used for
marketing or announcement attachments.

Verify the complete directory before use:

```powershell
./scripts/Verify-SessionDockDocumentationImages.ps1 `
    -AssetDirectory ./marketing/trusted/v2.7.0
```

Unredacted source captures are intentionally not retained. Do not add raw,
synthetic, generatively altered, or derivative files to this directory.
